{{ form(form) }}    {# pintar el formulario con la función form(), y vamos a pintar el formulario (form), que es esta variable llamada form que se encuentra en la action nuevoAction del archivo EventosController.php, es decir que hace referencia al formulario de EventosType.php #}